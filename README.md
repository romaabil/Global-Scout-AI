# 🌍 Global Scout AI

ИИ-агент, который исследует весь мир, чтобы найти для вас идеальное место.

## 🚀 Быстрый старт

### Установка

```bash
# Клонировать репозиторий (или создайте папку вручную)
# Перейдите в папку проекта
cd global-scout-ai

# Установите зависимости
pip install -r requirements.txt

# Запустите тестовый скрипт
python src/scout_core/scout.py

# Или запустите API
cd src/api
uvicorn main:app --reload --host 0.0.0.0 --port 8000
